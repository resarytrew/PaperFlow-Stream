name: Snapshot Chistovik release

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/extract-rebrand-error.yml

permissions:
  contents: write
  actions: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - name: Capture current release run
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          gh run list --repo "$GITHUB_REPOSITORY" --workflow release-personal-hub.yml --limit 5 --json databaseId,status,conclusion,headSha,url,createdAt > release-snapshot.json
      - name: Commit snapshot
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add release-snapshot.json
          git commit -m "chore: snapshot Chistovik release status"
          git pull --rebase origin main
          git push
