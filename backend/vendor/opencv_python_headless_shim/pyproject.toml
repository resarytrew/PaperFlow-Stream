[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "opencv-python"
version = "4.13.0.92"
description = "PaperFlow local shim: satisfy opencv-python dependencies while installing headless OpenCV."
requires-python = ">=3.11"
dependencies = [
    "opencv-python-headless==4.13.0.92",
]

[tool.setuptools]
packages = []
